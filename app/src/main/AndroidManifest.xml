<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.RentalMobilPNM"
        tools:targetApi="34">

        <!-- 1. MainActivity jadi Home utama (Car List) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Rental Mobil PNM">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 2. LoginActivity (kalau masih dipakai) -->
        <activity
            android:name=".LoginActivity"
            android:exported="false" />

        <!-- 3. Home (kelas lama) SUDAH TIDAK DIPAKAI → HAPUS SAJA -->
        <!-- Baris ini sudah saya komentari supaya aman dihapus -->
        <!--
        <activity
            android:name=".Home"
            android:exported="false" />
        -->

    </application>

</manifest>